export enum DonationType {
  FINANCIAL = 'financial',
  DELIVERY = 'delivery',
  IN_PERSON = 'in_person',
}

export enum WishStatus {
  PENDING = 'pending',
  IN_PROGRESS = 'in_progress',
  FULFILLED = 'fulfilled',
  CANCELLED = 'cancelled',
  EXPIRED = 'expired',
}
